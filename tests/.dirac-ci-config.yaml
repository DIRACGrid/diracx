config: {}

# List of feature variables which must be passed when preparing
required-feature-flags: []

# Additional services to use in docker-compose
extra-services: {}

# Additional shell commands to run
commands:
  post-prepare: []


# Location where diracx is installed in the container
install-location: /opt/conda/lib/python3.11/site-packages/diracx
